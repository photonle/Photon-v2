-- undecided on whether to implement this or not

if (exmeta.ReloadFile()) then return end

NAME = "PhotonInteractionSound"

local print = Photon2.Debug.Pring
local printf = Photon2.Debug.PrintF

local meta = exmeta.New()
