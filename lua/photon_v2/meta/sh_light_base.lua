if (exmeta.ReloadFile("photon_v2/meta/sh_light_base.lua")) then return end

NAME = "photon_light_base"

local Light = META

function Light:Initialize( component, id )

end

function Light:SetState( state, priority )

end